services:
  - type: web
    name: shadow-bot
    env: docker
    region: Frankfurt
    plan: free
    buildCommand: "npm install"
    startCommand: "npm run toxic"
    envVars:
      - key: PORT
        value: "4000"
